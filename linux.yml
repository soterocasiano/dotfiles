- name: Setup Linux environment
  hosts: local
  become: true
  tasks:
    - name: Install Homebrew dependencies
      apt:
        name: "{{ item }}"
        state: present
      loop:
        - build-essential
        - procps
        - curl
        - file
        - git

- import_playbook: install.yml
